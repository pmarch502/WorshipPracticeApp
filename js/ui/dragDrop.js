/**
 * Drag & Drop - DEPRECATED
 * This module is no longer used. Audio files are now loaded from static manifest.
 */

export function initDragDrop() {
    // No-op: drag & drop functionality has been removed
    // Audio files are now loaded from the manifest system
    console.log('Drag & drop disabled - use manifest-based loading');
}
